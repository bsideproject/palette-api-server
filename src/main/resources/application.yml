spring:
  main:
    banner-mode: off
  profiles:
    default: dev
    group:
      local: local
      dev: dev

management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: health, info
      path-mapping:
        health : healthcheck

server:
  port: 8082